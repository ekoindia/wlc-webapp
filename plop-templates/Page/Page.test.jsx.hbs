import { pageRender, screen, mockRouter } from "test-utils";
import {{pascalCase name}} from "{{pagefolder}}/{{lowerCase name}}";


describe("{{pascalCase name}}", () => {
	beforeEach(() => {
		mockRouter.setCurrentUrl("/page-path");
	});

	it("renders without error", () => {
		const { container } = pageRender(<{{pascalCase name}} />);
		expect(container).not.toBeEmptyDOMElement();
	});

	// it("matches snapshot", () => {
	//	const { asFragment } = pageRender(<{{pascalCase name}} />);
	// 	expect(asFragment()).toMatchSnapshot();
	// });

	test.todo("TODO: add proper test cases for {{pascalCase name}} in __tests__/{{pagefolder}}/{{lowerCase name}}/{{lowerCase name}}.test.jsx");
});
