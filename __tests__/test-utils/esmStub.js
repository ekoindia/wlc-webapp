// Minimal stub for ESM-only 3rd-party npm packages during Jest tests.
// Used to avoid errors like: "Jest encountered an unexpected token" or "Cannot use import statement outside a module"
// when such packages are imported in the code being tested.
// See jest.config.js for details.
// rules: tests, isolation
export default {};
