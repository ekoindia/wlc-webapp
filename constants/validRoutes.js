const publicLinks = ["/", "/test", "/oldicons", "/newicons"];

const baseRoute = {
	admin: "/admin",
};

const initialRoute = {
	admin: "/admin",
	"non-admin": "/home",
};

export { publicLinks, baseRoute, initialRoute };
